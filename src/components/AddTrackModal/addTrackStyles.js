import styled from 'styled-components';
import {
  ModalContainer,
  ModalTitle,
} from '../CreatePlaylist/createPlaylistStyles';

export const AddTrackContainer = styled(ModalContainer)`
  justify-content: flex-start;
  padding: 20px 0;
`;

export const AddTrackTitle = styled(ModalTitle)`
  font-size: 3.5rem;
`;
