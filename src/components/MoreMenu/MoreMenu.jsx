import React from 'react';
import { MoreMenuContainer } from './moreMenuStyles';

const MoreMenu = ({ open }) => {
  return open && <MoreMenuContainer>FDESSS</MoreMenuContainer>;
};

export default MoreMenu;
